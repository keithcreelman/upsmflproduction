{
  "version": "1.19.1",
  "released": "2026-06-30",
  "label": "v1.19.1 \u2014 Expected Points (xFP) tie-out fix: xFP is now summed over WEEKS 1-17 (our fantasy season) instead of 1-18, and the \u00b1 is computed against the OFFICIAL MFL Pts/PPG columns over MFL's game denominator \u2014 so xFP, xFP/g, \u00b1 and \u00b1/g all tie to the MFL numbers next to them (was: \u00b1 used a re-scored actual over a different game count, e.g. CMC showed xFP/g = MFL PPG yet \u00b1 = -20). (v1.19.0: Expected Points group added; v1.18.0: Route Separation from PFF.)",
  "changes_120": [
    "Horizontal scrollbar moved to the TOP of the table (synced two-way); the table now scrolls in a bounded 680px region so the scrollbar + column header stay fixed while the body scrolls (works in the iframe embed, not just standalone).",
    "Pos Rk now ranks within the IDP/offense GROUP (DL/LB/DB/QB/RB/WR/TE/PK) instead of the raw nflverse position \u2014 fixes multiple #1s showing in a single DL/LB/DB."
  ],
  "description": "The Advanced Stats Workbench gains a rank-within-position column (by MFL PPG, matching the mobile app), a coverage line that flags the 500-row cap and offseason, a 'Full profile' button in the drill-down drawer that opens the shared UPS player-profile modal, search by name/team/position, and a responsive layout for tablet/large-phone embeds. The leaderboard is also now available on the mobile site (focused per-position view).",
  "scope": {
    "this_release": [
      "Pos Rk column \u2014 rank within position by MFL PPG over the current query (client-side; mirrors mobile)",
      "Coverage line \u2014 'data through {season}', '500-cap' truncation warning, offseason 'hasn't started' note",
      "Full profile button in the drawer \u2192 shared player_profile_master.js modal (keeps the week/season charts)",
      "Search by name + NFL team + position (was name-only)",
      "Clearer MFL PPG tooltip",
      "Responsive layout for narrow/tablet widths"
    ],
    "deferred": [
      "Heat-map shading inside the drill-down drawer",
      "By-Season 'Total' row in the drawer",
      "Per-group PFR-2018+ / contract-is-current header notes",
      "Server-side IDP ordering (currently ordered by total yards)"
    ]
  }
}